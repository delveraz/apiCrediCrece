# TiDB Cloud / MySQL
DB_HOST=your-cluster.tidbcloud.com
DB_PORT=4000
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=microfinanzas_nica
DB_SSL=true

# Pool (opcional)
DB_POOL_SIZE=25

# Servidor
PORT=3000
API_HOST=0.0.0.0

# SKIP_STARTUP_TASKS=1  → no migraciones al arrancar (solo health + listen)
